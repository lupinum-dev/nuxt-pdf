// The PDF verification utilities are shipped from `src/test-utils/` (published as
// `@lupinum/nuxt-pdf/test`). This thin re-export keeps the whole existing suite
// exercising the same code users get, so there is one parser, not two.
export * from '../../src/test-utils/pdf'
