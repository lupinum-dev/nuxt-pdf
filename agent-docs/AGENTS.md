# @lupinum/nuxt-pdf

The complete documentation snapshot is generated from the public site during
package certification. Run `pnpm docs:build && pnpm docs:package` before you
inspect or pack the installed documentation from this source checkout.
